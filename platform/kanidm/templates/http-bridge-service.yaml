apiVersion: v1
kind: Service
metadata:
  name: kanidm-http-bridge
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app.kubernetes.io/name: kanidm-http-bridge
  ports:
    - name: http
      port: 8080
      targetPort: http
