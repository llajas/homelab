apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ddns.fullname" . }}-test-connection"
  labels:
    {{- include "ddns.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ddns.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
