apiVersion: v1
kind: Service
metadata:
  name: {{ include "ddns.fullname" . }}
  labels:
    {{- include "ddns.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ddns.selectorLabels" . | nindent 4 }}
