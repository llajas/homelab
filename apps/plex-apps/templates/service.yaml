apiVersion: v1
kind: Service
metadata:
  name: {{ include "plex-apps.fullname" . }}
  labels:
    {{- include "plex-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plex-apps.selectorLabels" . | nindent 4 }}
