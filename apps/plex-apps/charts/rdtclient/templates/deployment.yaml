apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rdtclient.fullname" . }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Chart.Name }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Chart.Name }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
      - name: {{ include "rdtclient.fullname" . }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: "{{ .Values.image.pullPolicy }}"
        ports:
        - containerPort: 6500
          name: http
        volumeMounts:
        - name: "{{ .Values.volumes.downloads.pvc.name }}"
          mountPath: "{{ .Values.volumes.downloads.pvc.mountPath }}"
        - name: "{{ .Values.volumes.db.pvc.name }}"
          mountPath: "{{ .Values.volumes.db.pvc.mountPath }}"
      volumes:
      - name: "{{ .Values.volumes.downloads.pvc.name }}"
        persistentVolumeClaim:
          claimName: "{{ .Values.volumes.downloads.pvc.name }}"
      - name: "{{ .Values.volumes.db.pvc.name }}"
        persistentVolumeClaim:
          claimName: "{{ .Values.volumes.db.pvc.name }}"
